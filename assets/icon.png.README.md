Drop a 300×300 transparent PNG named `icon.png` here before packaging with `pbiviz package`.

Suggested motif: a 5×7 calendar grid where four cells are filled with a green-to-dark-green gradient and three are empty — the visual signature of a calendar heatmap. Match GitHub's #216E39 → #EBEDF0 palette for instant recognition.
